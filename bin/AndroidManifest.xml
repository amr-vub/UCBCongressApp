<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.ucb.ui"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="17"
        android:targetSdkVersion="19" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.LoadingActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.RegisterActivity" 
            android:label="Register"/>
        <activity android:name=".activity.StartActivity" />
        <activity android:name=".activity.InvitationActivity"
            android:label="Invitation"/>
        <activity android:name=".activity.home.AgendaActivity" 
            android:label="Agenda"/>
        <activity android:name=".activity.home.HomeActivity" 
            android:label="Agenda"/>
        <activity android:name=".activity.home.SpeakersActivity" 
            android:label="Speakers"/>
        <activity android:name=".activity.session.SessionActivity" />
        <activity android:name=".activity.session.ViewDocumentActivity" />
        <activity android:name=".activity.home.PreferenceActivity" 
            android:label="Preferences"/>
        <activity
            android:name=".activity.home.UserProfileActivity"
            android:label="Profile" />
        <activity android:name=".activity.survey.AnonySurveyActivity1" />
        <activity android:name=".activity.survey.AnonySurveyActivity2" />
        <activity android:name=".activity.survey.AnonySurveyActivity3" />
        <activity android:name=".activity.learnmore.LearnMoreActivity" />
    </application>

</manifest>